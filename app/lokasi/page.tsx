import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function LokasiPage() { const units = await db.unit.findMany({ where: { isActive: true }, orderBy: [{ sortOrder: "asc" }, { name: "asc" }] }); return <main className="min-h-screen bg-emerald-50"><header className="border-b border-emerald-100 bg-white"><div className="mx-auto max-w-6xl px-6 py-5"><a href="/" className="text-2xl font-extrabold text-emerald-700">ayo belajar</a></div></header><div className="mx-auto max-w-6xl px-6 py-16"><p className="text-sm font-bold uppercase tracking-[.22em] text-emerald-700">Ayo Belajar</p><h1 className="mt-2 text-4xl font-bold text-slate-950">Lokasi unit kami</h1><div className="mt-10 grid gap-8 md:grid-cols-2">{units.map((unit) => <article key={unit.id} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><span className="text-xs font-bold text-emerald-700">{unit.isHeadOffice ? "KANTOR PUSAT" : "UNIT BELAJAR"}</span><h2 className="mt-2 text-2xl font-bold text-slate-950">{unit.name}</h2><p className="mt-3 text-slate-600">{unit.address}</p>{unit.description && <p className="mt-4 text-slate-600">{unit.description}</p>}{unit.mapEmbedUrl ? <iframe title={`Peta ${unit.name}`} src={unit.mapEmbedUrl} className="mt-6 h-80 w-full rounded-2xl border-0" loading="lazy" referrerPolicy="no-referrer-when-downgrade" allowFullScreen /> : <div className="mt-6 rounded-2xl bg-emerald-50 p-8 text-emerald-900">Peta belum diatur oleh admin.</div>}</article>)}</div></div></main>; }
