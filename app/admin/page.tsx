import { auth, signOut } from "@/lib/auth";
import { redirect } from "next/navigation";
import Link from "next/link";
export default async function AdminPage() { const session = await auth(); if (!session?.user) redirect("/admin/login"); return <main className="min-h-screen bg-slate-50"><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5"><Link href="/admin" className="text-2xl font-extrabold text-emerald-700">ayo belajar admin</Link><form action={async () => { "use server"; await signOut({ redirectTo: "/admin/login" }); }}><button className="rounded-lg border px-3 py-2 text-sm">Keluar</button></form></div></header><main className="mx-auto max-w-6xl px-6 py-12"><p className="text-sm text-slate-500">Masuk sebagai {session.user.name}</p><h1 className="mt-2 text-3xl font-bold">Dashboard</h1><div className="mt-8 grid gap-5 md:grid-cols-2"><Link href="/admin/units" className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="text-xl font-bold text-emerald-700">Kelola Unit & Google Maps</h2><p className="mt-2 text-slate-600">Tambah, ubah, dan hapus lokasi unit lembaga.</p></Link><Link href="/admin/certificates" className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="text-xl font-bold text-emerald-700">Cetak Sertifikat</h2><p className="mt-2 text-slate-600">Buat sertifikat dengan data lembaga dan tanda tangan.</p></Link></div></main></main>; }
