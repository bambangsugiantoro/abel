"use client";
import { signIn } from "next-auth/react";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function LoginPage() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const router = useRouter(); async function submit(e: React.FormEvent) { e.preventDefault(); setLoading(true); setError(""); const result = await signIn("credentials", { email, password, redirect: false }); setLoading(false); if (result?.error) setError("Email atau kata sandi tidak valid."); else router.push("/admin"); } return <main className="grid min-h-screen place-items-center bg-emerald-50 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-xl shadow-emerald-900/10"><a href="/" className="text-2xl font-extrabold text-emerald-700">ayo belajar</a><h1 className="mt-7 text-3xl font-bold">Masuk Admin</h1><p className="mt-2 text-sm text-slate-600">Gunakan akun admin yang telah dibuat saat proses seed.</p>{error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<label className="mt-6 block text-sm font-bold">Email<input required type="email" value={email} onChange={e => setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" /></label><label className="mt-4 block text-sm font-bold">Kata sandi<input required type="password" minLength={12} value={password} onChange={e => setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" /></label><button disabled={loading} className="mt-6 w-full rounded-xl bg-emerald-700 px-4 py-3 font-bold text-white disabled:opacity-60">{loading ? "Memproses..." : "Masuk"}</button></form></main>; }
