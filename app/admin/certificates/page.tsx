"use client";
import { useState } from "react";
export default function CertificatesPage() { const [message, setMessage] = useState(""); const [url, setUrl] = useState(""); const [form, setForm] = useState({ studentName: "", programName: "", description: "Telah menyelesaikan program pembelajaran dengan baik.", issuedAt: new Date().toISOString().slice(0, 10), institutionName: "Ayo Belajar", directorName: "Zulaika", logoUrl: "", signatureUrl: "", template: "CLASSIC_GREEN" }); const set = (k: string, v: string) => setForm(f => ({ ...f, [k]: v })); async function submit(e: React.FormEvent) { e.preventDefault(); setMessage(""); const r = await fetch("/api/admin/certificates", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await r.json(); if (!r.ok) { setMessage(data.error || "Gagal membuat sertifikat."); return; } const link = `/sertifikat/${data.verificationCode}`; setUrl(link); setMessage("Sertifikat berhasil dibuat."); } return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-3xl"><a href="/admin" className="font-bold text-emerald-700">← Dashboard</a><h1 className="mt-4 text-3xl font-bold">Buat Sertifikat</h1><p className="mt-2 text-slate-600">Logo dan tanda tangan diisi sebagai URL gambar HTTPS. Untuk produksi gunakan Cloudinary, Vercel Blob, atau storage lain.</p><form onSubmit={submit} className="mt-8 rounded-2xl bg-white p-6 shadow-sm"><div className="grid gap-4">{[["studentName", "Nama siswa"], ["programName", "Nama program"], ["institutionName", "Nama lembaga"], ["directorName", "Nama direktur"], ["logoUrl", "URL logo (opsional)"], ["signatureUrl", "URL tanda tangan (opsional)"]].map(([k, label]) => <label key={k} className="text-sm font-bold">{label}<input value={(form as any)[k]} onChange={e => set(k, e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" /></label>)}<label className="text-sm font-bold">Tanggal terbit<input required type="date" value={form.issuedAt} onChange={e => set("issuedAt", e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" /></label><label className="text-sm font-bold">Keterangan sertifikat<textarea required minLength={10} rows={5} value={form.description} onChange={e => set("description", e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" /></label><button className="rounded-xl bg-emerald-700 px-4 py-3 font-bold text-white">Buat Sertifikat</button></div></form>{message && <div className="mt-5 rounded-xl bg-emerald-50 p-4 text-emerald-900">{message}{url && <><br /><a className="font-bold underline" href={url} target="_blank">Buka dan cetak sertifikat</a></>}</div>}</div></main>; }
